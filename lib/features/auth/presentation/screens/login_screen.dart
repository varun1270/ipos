import 'auth_shell_screen.dart';

/// Login entry point — renders the shared auth shell on the login tab.
class LoginScreen extends AuthShellScreen {
  const LoginScreen({super.key}) : super(initialTab: 0);
}
