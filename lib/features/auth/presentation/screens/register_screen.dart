import 'auth_shell_screen.dart';

/// Register entry point — renders the shared auth shell on the register tab.
class RegisterScreen extends AuthShellScreen {
  const RegisterScreen({super.key}) : super(initialTab: 1);
}
