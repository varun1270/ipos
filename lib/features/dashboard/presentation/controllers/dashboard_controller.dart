// Top-level controller for the dashboard screen.
// Orchestrates initial load and pull-to-refresh by invoking the dashboard
// use cases together, and exposes a single combined view state to the UI.
